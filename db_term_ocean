on run {input, parameters}
	tell application "Terminal" to do script "ssh db"
	tell application "Terminal" to set current settings of front window to (first settings set whose name is "Ocean")
end run
