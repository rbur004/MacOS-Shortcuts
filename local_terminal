on run {input, parameters}
	tell application "Terminal" to do script "" activate
	tell application "Terminal" to set current settings of front window to (first settings set whose name is "Man Page")
end run
